# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=alhosni_saas

# Minio
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_BUCKET=uploads
MINIO_USE_SSL=false

# Backend
PORT=4000
JWT_SECRET=change-this-secret-in-production
# DATABASE_URL is constructed automatically in docker-compose, OR override here:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/alhosni_saas

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000
